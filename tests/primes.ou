main Primes   /* Prints all prime numbers between 2 to 38 */

  var a, b: int; 
           stop: int; 
start

  print (2);
  print (3);
  a := 5;
  
  while(a <= 38) {
    b := 2;
    stop := 1;

    while(b < stop*a/2) {
       if (a/b * b==a) then { stop := 0;} 
	   else { }
       b := b + 1;
    }
    if (stop == 1) then 
	    { print(a);} 
	else { }
    a := a + 2;
  }

trats
