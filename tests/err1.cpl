main errors  /* a program with errors */


  var i, j: int;
      a, b, a: real ; /* a declared twice */

start
   i := b;  /* real ==> int */

   read (foo);  /* foo is not defined */ 
   
   if (b >= 10.0) then {
 
       b := b + 17.0;
       print (b);
   }
   else {
        b := b + 1;
        (i * a);  /* syntax error */
   }

   i := 20.0; /* real => int */

     
trats
