i, j: int;
a, b: float;
{
   input (a);
   input (i);
   
   
   if (a >= i) {
 
       b = a - i;
       output (b);
   }
   else {
       output (i * a);
   }
  
   i = 3 + 5;
   output (i);
   
   a = 3 + 5;
   output (a);
   
   a = 3.0 + 5.0;
   output (a);
}
